{
  "name": "parse-argv",
  "version": "0.0.6",
  "description": "Parse argument vectors",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/distify-cli/cli.js --input-file=./index.js --output-dir=./dist --is-node",
    "prepublish": "npm run build"
  },
  "author": "Joe Gesualdo",
  "license": "MIT",
  "dependencies": {
    "@joegesualdo/terminal-log": "0.0.5",
    "distify-cli": "0.0.8"
  }
}
